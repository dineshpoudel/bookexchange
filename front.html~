<!DOCTYPE html>

<html>
  <head>
    <title> {{ mainpageTitle }} </title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="static/main.css">
	<script src="static/script.js"></script>
  </head>
	
  <body>
    <div class="bookSearch" > 
	<form method="post" action="/findBook">
	<label name="findBook">Search Book &nbsp;&nbsp;<input type="search" name="findBook" placeholder="Press Enter to search"></label>
	</form>
</div>



    {% block content %}
    {{ message }}				
    <div class="addBook" > Click Here to add the book you want to sell or donate 
      <form method="post" action="/">
	<table class="table_form">
	<label id="bookTitle"> Book Title(*):
	  <input type="text" name="bookTitle" value="{{ newBook.bookTitle }}"> {{ bookTitleError }}
	</label>
	<br>

	<label id="bookCondition"> Book condition(*):
	  <textarea name="bookCondition" >{{ newBook.bookCondition }}</textarea>
	  {{ bookConditionError }}
	</label>
	<br>

	<label id="price"> Asking Price(*):
	  <input type="text" name="price"  value="{{ newBook.price }}"> {{ priceError }}
	</label>
	<br>
<i>Please provide at least one way to contact you</i><br>
	<label id="phoneNumber"> Phone Number:
	  <input type="text" name="phoneNumber" value="{{ newBook.phoneNumber }}">
	</label>
	<br>

	<label id="email"> Email address:
	  <input type="text" name="email" value="{{ newBook.email }}">
	</label>
	<br>

	<label id="facebook"> Facebook link:
	  <input type="text" name="facebook" value="{{ newBook.facebook }}">
	</label>{{ contactError }}
	<br>

<i>Enter more details (make this collapsible) </i><br>
	<label id="courseID"> Course:
 	  <input type="text" name="courseID" value="{{ newBook.courseID }}">
	</label>
	<br>

	<label id="ISBN"> ISBN:
	  <input type"text" name="ISBN" value="{{ newBook.ISBN }}">
	</label>
	<br>

	<label id="edition"> Edition:
	  <input type="text" name="edition" value="{{ newBook.edition }}">
	</label>
	<br>


	<label id="remark"> Any Remarks:
	  <textarea name="remark">{{ newBook.remark }} </textarea>
	</label>
	<br>
	
	<input type="hidden" name="googleImage" value=""> 
	<input type="submit" name="submit" value="{{ submit }}">
	<input type="button" name="reset" onclick="{{resetAction}}" value="{{ reset }}">
	<br>
	</table>
      </form>
    </div>	
	<div id="googleImgaes"> Select one of the following images <hr> 
{% for image in googleImages %}
<img src="{{ image }}" height=120px></img> 
{% endfor %}
<hr> </div>
    <div id="featuredBooks" >
			<b>Featured Books</b><hr> 
	{% for book in featuredBooks %}
		{{ book.bookTitle }}
		<br>
	{% endfor %}	
    </div>
    {% endblock %}	
<hr>	
    <div class="heading" ><h2>Book Exchange</h2> </div>
  </body>
</html>
